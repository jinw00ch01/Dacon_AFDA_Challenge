{
  "_note": "S3 e004 (decision 4fb9418c): base = e001 accel-argmax head (predict_speed=false; steer_macro_f1 0.408 not collapsed, best steer of e001/e002/e003), SINGLE change = predict_stopped=true. e001/e002 predict STOPPED 0 times -> accel Macro-F1 capped at 0.75 (STOPPED F1=0). A dedicated binary STOPPED head (BCEWithLogits, pos_weight for the rare positives) overrides the argmax accel class to STOPPED where sigmoid(logit)>=stopped_threshold. Pro selftest 918be48a: a well-calibrated STOPPED head lifts official_s3 ~0.49->0.68 (loso_stitched). Base is argmax so val_predictions.csv now carries accel_pred_raw (pre-override) for Pro's independent threshold gate; the predict_stopped=off base run is e001 (models/stage3/val_predictions.csv, official_s3 0.2495). out_dir isolated so v001 stage3 (e001) is preserved; promote stage3_e004->stage3 only if e004 gate passes (official_s3>base AND STOPPED F1>0 AND steer not collapsed). Proxy targets (v1b rule) are self-diagnostic, not official GT; leaderboard S3 (0.4656) is the external judge.",
  "experiment_id": "s3-e004",
  "stage": "stage3",
  "cache_dir": "data/derived/s3_clip_cache_v1",
  "aux_csv": "data/derived/releases/s3-aux-rules-v1b-20260925/s3_auxiliary_10hz.csv",
  "rule": {"window": 5, "v_stop": 0.5, "a_db": 0.2, "s_th": 4.5, "bias": -0.3},
  "predict_speed": false,
  "predict_stopped": true,
  "stopped_loss_weight": 1.0,
  "stopped_threshold": 0.5,
  "epochs": 3,
  "batch_size": 2,
  "grad_accum": 4,
  "lr": 3e-4,
  "weight_decay": 0.05,
  "seed": 0,
  "num_workers": 4,
  "window": 16,
  "train_stride": 2,
  "val_stride": 1,
  "init_pretrained": true,
  "amp": true,
  "out_dir": "models/stage3_e004"
}
